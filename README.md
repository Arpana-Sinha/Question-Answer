# 📄 Document Q&A — Gradio App

This is a simple **Question Answering (Q&A)** application built using:

* **Python**
* **Gradio**
* **HuggingFace Transformers**
* **RoBERTa (SQuAD2)** model

Upload any **text file**, ask a question, and the model will extract the answer from the document.

---

## 🚀 Features

* Upload `.txt` document
* Automatically read file content
* Ask any question based on the document
* Model extracts best answer
* Simple Gradio UI
